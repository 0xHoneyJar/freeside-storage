{
  "name": "@0xhoneyjar/storage",
  "version": "0.0.1",
  "private": true,
  "description": "freeside-* attachment module for storage layout + static-asset CDN + NFT metadata serving + retrieval API. Closes loa-freeside#167. Lean instance-1 ships protocol + S3 adapter (per ADR-001).",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/0xHoneyJar/freeside-storage.git"
  },
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/pg": "^8.11.11",
    "effect": "^3.10.0",
    "jsdom": "^24.1.0",
    "pg": "^8.20.0",
    "tsx": "^4.19.4",
    "typescript": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
