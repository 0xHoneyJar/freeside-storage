// Re-exports for the schema layer.
// Sealed Effect Schema sub-modules — populated by T0-2.
// Empty placeholder until asset-reference / consumer-constraint /
// asset-variant / asset-error land.
export {};
