{
  "name": "@freeside-storage/protocol",
  "version": "0.0.1",
  "private": true,
  "description": "Sealed schemas for freeside-storage — StorageAdapter common interface + URL contract + types. Lean instance-1 (per ADR-001).",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "effect": "^3.10.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
