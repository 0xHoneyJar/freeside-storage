{
  "name": "@freeside-filesystem/protocol",
  "version": "0.0.1",
  "private": true,
  "description": "STUB — sealed schemas (per-token metadata format, storage layout, retrieval API, CDN config). Content pending parallel design session.",
  "license": "MIT"
}
